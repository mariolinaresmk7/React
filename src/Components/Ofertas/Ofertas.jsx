// import { useEffect } from "react";
// const { ofertas } = useParams();
// const Ofertas = () => {
//   useEffect(() => {
//     const productRef = query(collection(db, "productos"),);
//     const q = categoryId
//       ? query(
//           productRef,
//           where("ofertas", "==", ofertas),
//         )
//       : productRef;
//     getDocs(q).then((resp) => {
//       const productosDB = resp.docs.map((doc) => ({
//         id: doc.id,
//         ...doc.data(),
//       }));
//       setProduct(productosDB);
//     });
//   }, []);
//   return <>
//   </>;
// };
// export default Ofertas;
